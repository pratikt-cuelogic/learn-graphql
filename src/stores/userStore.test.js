import { userStore } from "./userStore"

describe("userStore", () => {
  it("creates new user", () => {
  	// console.log(userStore);
    // const store = new userStore;
    // expect(0).toBe(0);

    // const u = {"name":"Sims Salas","email":"simssalas@bulljuice.com","city":"Shawmut","state":"Puerto Rico","ssn":"(955) 582-3266","dateOfBirth":"1990-04-04"};
    // store.createUser(u.name, u.email, u.city, u.state, u.ssn, u.dateOfBirth)
    // expect(store.users.length).toBe(1);
  });
});